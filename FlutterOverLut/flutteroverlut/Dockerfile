# Placeholder for Flutter web - builds quickly without full Flutter SDK
FROM nginx:alpine
RUN echo "Flutter web build placeholder - configure Flutter build in pubspec.yaml and run 'flutter build web' locally first" > /usr/share/nginx/html/index.html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
